# DSCPP_PBL_SEM_III
Skip List–based in-memory Key-Value Store in C++, built as a DSA + OOP PBL. Implements a templated, generic Skip List from scratch (insert/search/delete, O(log n) expected) with a SET/GET/DEL/RANGE CLI on top, benchmarked against std::map and unordered_map.
